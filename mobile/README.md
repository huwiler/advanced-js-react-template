# Mobile App (Expo + React Native)

This folder will contain your **mobile application**, created in **Weeks 4–7**.

To initialize:

```bash
cd mobile
npx create-expo-app .
npm install
npm run start
```

## Notes

- Do not run `npm install` at the repo root
- Do not commit build folders (`ios/build`, `android/app/build`, etc.).